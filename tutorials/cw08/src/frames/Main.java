package frames;

import javax.swing.*;

public
    class Main {
    public static void main(String[] args) {

        // SwingUtilities.invokeLater(() -> new frames.JButtonFrame());
        // SwingUtilities.invokeLater(() -> new frames.JCheckBoxFrame());
        // SwingUtilities.invokeLater(() -> new frames.JRadioButtonFrame());
        // SwingUtilities.invokeLater(() -> new frames.JLabelFrame());
        // SwingUtilities.invokeLater(() -> new frames.JTextFieldFrame());
        // SwingUtilities.invokeLater(() -> new frames.JSliderFrame());
        // SwingUtilities.invokeLater(() -> new frames.JSplitPaneFrame());
        // SwingUtilities.invokeLater(() -> new frames.JTabbedPaneFrame());

        // new JOptionPaneFrame();

    }
}
